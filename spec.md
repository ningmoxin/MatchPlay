# 專案規格書 (Project Specification)

## 1. 專案概述

### 1.1 專案名稱
MatchPlay

### 1.2 專案目標
透過index.html 連結 firebase，建立一個配對系統在聖誕交換時禮物使用

### 1.3 目標使用者
管理者執行配對，其他人透過手機也可以同時查看動畫

---

## 2. 功能需求

### 2.1 核心功能

| 功能名稱 | 描述 |
|---------|------|
| 參與者管理 | 管理者可預先輸入參與者名單 |
| 雙向配對 | 系統自動將參與者兩兩配對（A↔B、C↔D），互相交換禮物 |
| 配對動畫 | 逐一揭曉配對結果，有抽獎感的動畫效果 |
| 即時同步 | 透過 Firebase 即時同步，多人可同時在手機上觀看動畫 |
| 配對控制 | 管理者控制何時開始配對、何時揭曉下一組 |

### 2.2 使用者故事

1. 作為 **管理者**，我希望 **預先輸入所有參與者名單**，以便 **活動當天直接進行配對**
2. 作為 **管理者**，我希望 **控制配對揭曉的節奏**，以便 **營造活動氣氛**
3. 作為 **管理者**，我希望 **執行隨機配對**，以便 **公平分配交換對象**
4. 作為 **觀眾**，我希望 **在手機上同步觀看配對動畫**，以便 **參與活動的樂趣**
5. 作為 **參與者**，我希望 **清楚看到我的配對對象是誰**，以便 **知道要和誰交換禮物**

### 2.3 使用流程

```
[活動前]
管理者 → 開啟管理頁面 → 輸入參與者名單 → 儲存至 Firebase

[活動中]
1. 管理者 → 點擊「開始配對」→ 系統隨機兩兩配對
2. 管理者 → 點擊「揭曉下一組」→ 動畫顯示一組配對結果
3. 觀眾手機 → 同步顯示相同動畫
4. 重複步驟 2-3 直到所有配對揭曉完畢

[活動後]
所有人可查看完整配對結果
```

---

## 3. 技術規格

### 3.1 技術棧
- **前端**：HTML + CSS + JavaScript（純前端，無框架）
- **後端**：無（Serverless）
- **資料庫**：Firebase Realtime Database
- **其他**：Firebase Hosting（可選）、QR Code 產生器

### 3.2 系統架構

```
┌─────────────────────────────────────────────────────┐
│                    Firebase                          │
│  ┌─────────────────┐  ┌─────────────────────────┐   │
│  │ Realtime DB     │  │ Hosting (optional)      │   │
│  │ - participants  │  │ - index.html            │   │
│  │ - pairs         │  │ - admin.html            │   │
│  │ - gameState     │  │ - viewer.html           │   │
│  └────────┬────────┘  └─────────────────────────┘   │
└───────────┼─────────────────────────────────────────┘
            │ 即時同步
    ┌───────┴───────┐
    ▼               ▼
┌────────┐    ┌────────────┐
│ 管理者  │    │ 觀眾 (多人) │
│ 電腦    │    │ 手機       │
└────────┘    └────────────┘
```

### 3.3 頁面結構

| 頁面 | 檔案 | 用途 |
|-----|------|------|
| 首頁 | index.html | 入口頁面，選擇進入管理或觀看 |
| 管理頁 | admin.html | 管理者輸入名單、執行配對、控制揭曉 |
| 觀看頁 | viewer.html | 觀眾同步觀看配對動畫 |

### 3.4 Firebase 資料結構

```javascript
{
  "config": {
    "adminPassword": "預設密碼（hash）"
  },
  "participants": {
    "id1": { "name": "小明", "order": 1 },
    "id2": { "name": "小華", "order": 2 }
    // ...
  },
  "pairs": {
    "pair1": { "person1": "id1", "person2": "id2", "revealed": false },
    "pair2": { "person1": "id3", "person2": "id4", "revealed": false }
    // ...
  },
  "gameState": {
    "status": "waiting", // waiting | pairing | revealing | finished
    "currentPairIndex": 0,
    "lastUpdated": timestamp
  }
}

---

## 4. 資料模型

### 4.1 實體定義

#### Participant（參與者）
| 欄位名稱 | 類型 | 必填 | 描述 | 驗證規則 |
|---------|------|-----|------|---------|
| id | string | 是 | 唯一識別碼 | Firebase 自動產生 |
| name | string | 是 | 參與者姓名 | 1-20 字元，不可重複 |
| order | number | 是 | 顯示順序 | 正整數 |

#### Pair（配對組）
| 欄位名稱 | 類型 | 必填 | 描述 | 驗證規則 |
|---------|------|-----|------|---------|
| id | string | 是 | 唯一識別碼 | Firebase 自動產生 |
| person1 | string | 是 | 第一位參與者 ID | 必須存在於 participants |
| person2 | string | 是 | 第二位參與者 ID | 必須存在於 participants |
| revealed | boolean | 是 | 是否已揭曉 | 預設 false |
| revealOrder | number | 是 | 揭曉順序 | 正整數 |

#### GameState（遊戲狀態）
| 欄位名稱 | 類型 | 必填 | 描述 | 驗證規則 |
|---------|------|-----|------|---------|
| status | string | 是 | 目前狀態 | waiting/pairing/revealing/finished |
| currentPairIndex | number | 是 | 目前揭曉到第幾組 | 0 起始 |
| lastUpdated | timestamp | 是 | 最後更新時間 | Firebase 時間戳 |

### 4.2 狀態流程

```
waiting → pairing → revealing → finished
   ↑                              │
   └──────── reset ───────────────┘
```

| 狀態 | 說明 |
|-----|------|
| waiting | 等待中，可編輯參與者名單 |
| pairing | 正在執行配對演算法 |
| revealing | 逐一揭曉配對結果中 |
| finished | 所有配對已揭曉完畢 |

---

## 5. 介面設計

### 5.1 頁面列表

| 頁面名稱 | 檔案 | 描述 |
|---------|------|------|
| 首頁 | index.html | 選擇角色入口（管理者/觀眾） |
| 管理頁 | admin.html | 管理者操作介面 |
| 觀看頁 | viewer.html | 觀眾觀看配對動畫 |

### 5.2 頁面詳細設計

#### 首頁 (index.html)
```
┌─────────────────────────────┐
│      🎄 MatchPlay 🎄        │
│     聖誕交換禮物配對         │
│                             │
│  ┌─────────┐ ┌─────────┐   │
│  │  管理者  │ │  觀眾   │   │
│  │  入口   │ │  入口   │   │
│  └─────────┘ └─────────┘   │
│                             │
│      [顯示 QR Code]         │
└─────────────────────────────┘
```

#### 管理頁 (admin.html)
```
┌─────────────────────────────────────┐
│ [← 返回]     管理介面     [登出]    │
├─────────────────────────────────────┤
│ 📋 參與者名單                        │
│ ┌─────────────────────────────────┐ │
│ │ 1. 小明  [✏️][🗑️]               │ │
│ │ 2. 小華  [✏️][🗑️]               │ │
│ │ 3. 小美  [✏️][🗑️]               │ │
│ │ [+ 新增參與者]                   │ │
│ └─────────────────────────────────┘ │
│                                     │
│ 目前狀態: ⏳ 等待中                  │
│                                     │
│ ┌─────────────┐ ┌─────────────┐    │
│ │  開始配對   │ │  重置全部   │    │
│ └─────────────┘ └─────────────┘    │
│                                     │
│ --- 配對結果 ---                    │
│ 組 1: 小明 ↔ 小美 [揭曉]           │
│ 組 2: 小華 ↔ 小強 [等待中]         │
│                                     │
│ [揭曉下一組]                        │
└─────────────────────────────────────┘
```

#### 觀看頁 (viewer.html)
```
┌─────────────────────────────┐
│      🎄 配對揭曉中 🎄        │
│                             │
│    ┌─────────────────┐     │
│    │                 │     │
│    │   🎁 動畫區域 🎁  │     │
│    │                 │     │
│    │  小明 ↔ 小美    │     │
│    │                 │     │
│    └─────────────────┘     │
│                             │
│   已揭曉: 1 / 3 組          │
│                             │
│   --- 已揭曉配對 ---        │
│   組 1: 小明 ↔ 小美         │
└─────────────────────────────┘
```

### 5.3 UI/UX 需求

| 項目 | 需求 |
|-----|------|
| 配色 | 聖誕節風格（紅、綠、金） |
| 響應式 | 支援手機與電腦瀏覽 |
| 動畫 | 揭曉時有抽獎感的動畫效果（如：名字滾動、閃爍、彈出） |
| 音效 | 可選：揭曉時播放音效 |
| 即時性 | 管理者操作後，觀眾頁面即時更新（< 1 秒） |

---

## 6. 非功能性需求

### 6.1 效能需求
- 即時同步延遲 < 1 秒
- 支援同時 50+ 觀眾連線
- 頁面載入時間 < 3 秒

### 6.2 安全性需求
- 管理頁面需輸入簡單密碼驗證
- 密碼以 hash 形式儲存
- Firebase 安全規則限制寫入權限

### 6.3 可用性需求
- 支援 Chrome、Safari、Edge 等主流瀏覽器
- 響應式設計，支援手機與電腦
- 無需安裝 App，純網頁操作

---

## 7. 限制與假設

### 7.1 已知限制
- 參與者人數需為偶數（雙向配對）
- 單場活動設計，不支援多場次
- 需要網路連線才能使用

### 7.2 假設條件
- 使用者已有 Firebase 專案
- 管理者會事先準備好參與者名單
- 活動現場有穩定網路環境

---

## 8. 里程碑

| 階段 | 內容 | 交付項目 |
|-----|------|---------|
| MVP | 基本配對功能 | index.html、admin.html、viewer.html、Firebase 連接 |
| V1 | 完整動畫與體驗 | 揭曉動畫、QR Code、響應式 UI、音效（可選） |

---

## 9. 開發前置作業

### 9.1 Firebase 設定 ✅

```javascript
const firebaseConfig = {
  apiKey: "AIzaSyCrrm45SzhPgLtkqspiXkpWC_rb0Q6MnAM",
  authDomain: "matchplay-8a324.firebaseapp.com",
  databaseURL: "https://matchplay-8a324-default-rtdb.asia-southeast1.firebasedatabase.app",
  projectId: "matchplay-8a324",
  storageBucket: "matchplay-8a324.firebasestorage.app",
  messagingSenderId: "477387192568",
  appId: "1:477387192568:web:5cdd491ee498f7ab03e30d"
};
```

### 9.2 管理者密碼 ✅
- 密碼：`admin2025`

### 9.3 部署方式 ✅
- 部署平台：**GitHub Pages**
- 需建立 GitHub repository 並啟用 Pages 功能

---

## 10. 附錄

### 10.1 名詞解釋

| 名詞 | 定義 |
|-----|------|
| 雙向配對 | 兩人互相交換禮物，A↔B 表示 A 送 B 且 B 送 A |
| 揭曉 | 顯示一組配對結果的動作 |
| 即時同步 | 透過 Firebase 讓多裝置同時看到相同內容 |
